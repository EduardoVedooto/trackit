import styled from "styled-components";

const Hoje = () => {
    return(
        <HojeContainer>
            HOJE
        </HojeContainer>
    );
}

const HojeContainer = styled.main`
    background-color: #E5E5E5;
    height: 100vh;
    padding: 70px 0;
`;

export default Hoje;